# Minimal

The absolute bare essentials needed to run a reconstruction. Best for quick tests, tutorials, or if you want to rely entirely on PtyRAD's default behaviors.

```{literalinclude} minimal.yaml
:language: yaml
:linenos:
```
